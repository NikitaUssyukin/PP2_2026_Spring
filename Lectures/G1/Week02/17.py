# Strings
# another way to use single or double quotes inside a string
a = 'which is escaping a character using a backslash - \" or \''
# escaping a character using a backslash is used with special characters
print(a)